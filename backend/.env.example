# Environment configuration for YouTube Summarizer Backend

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# Model Configuration
MODEL_PATH=llama-2-7b-32k-instruct.Q4_K_S.gguf
USE_GPU=false
MAX_TOKENS=512
TEMPERATURE=0.1
TOP_P=0.95
CONTEXT_SIZE=2048

# Whisper Configuration
WHISPER_MODEL_SIZE=base

# Storage Configuration
DOWNLOAD_DIR=downloads
CLEANUP_TEMP_FILES=true

# Logging Configuration
LOG_LEVEL=INFO

# CORS Configuration (for production, specify exact origins)
ALLOWED_ORIGINS=*

# Rate Limiting (requests per minute)
RATE_LIMIT=10
